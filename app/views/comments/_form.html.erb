<%= simple_form_for [post, comment], remote: true do |f| %>
  <%= f.input :body, label:'Enter your comment.', input_html: { class: 'form-control' }, wrapper_html: { class: 'form-group' } %>
  <%= f.button :submit, 'Submit', class: 'btn btn-success' %>
<% end %>   